# Local Reference Cache Notice

The peer-review trial app includes the bundled local celldex reference cache. This source-only directory intentionally contains a notice instead of duplicating the cache. If rebuilding from source, copy the prepared `celldex_cache` directory into the project root or allow the R reference packages to create a cache with network access.
