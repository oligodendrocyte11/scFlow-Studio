# License Public Key

This directory is reserved for the public key used by the academic trial licensing system in packaged builds.

The actual `public_key.pem` file is not included in the public source-code snapshot.

For public releases, the packaged macOS `.app` contains the runtime resources required for reviewer testing. Source builds that require licensing resources should restore the appropriate public key file during the private build process.
